server:
  port: 8080

spring:
  cloud:
    gateway:
      discovery:
        locator:
          enabled: false
      routes:
        - id: main-service
          uri: http://ewm-service:8080
          predicates:
            - Path=/**