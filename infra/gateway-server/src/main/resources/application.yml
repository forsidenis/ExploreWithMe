server:
  port: 8080

spring:
  application:
    name: gateway-server
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
          lower-case-service-id: false
      routes:
        - id: main-service
          uri: lb://MAIN-SERVICE
          predicates:
            - Path=/**